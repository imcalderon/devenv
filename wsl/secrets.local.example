# WSL Bootstrap Secrets — Local overrides
# Copy this file to secrets.local and fill in your values.
# secrets.local is .gitignored and will NEVER be committed.
#
# These values are used by:
#   - install-wsl.ps1 / reset-wsl.ps1 (WSL user creation)
#   - bootstrap-wsl.sh (secrets.env generation inside WSL)
#
# Usage: cp secrets.local.example secrets.local && edit secrets.local

# --- WSL User ---
WSL_USERNAME="devuser"
WSL_PASSWORD="devenv"
WSL_TIMEZONE="America/Chicago"

# --- Git ---
GIT_USER_NAME=""
GIT_USER_EMAIL=""

# --- API Keys ---
GITHUB_TOKEN=""
ANTHROPIC_API_KEY=""

# --- Optional ---
# VAULT_ADDR=""
# VAULT_TOKEN=""
