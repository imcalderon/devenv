# /dev — Start development server

## Steps
1. Ensure dependencies installed: `npm install` (if node_modules missing)
2. Start dev server: `npm run dev`
3. Report the local URL (usually http://localhost:5173)
