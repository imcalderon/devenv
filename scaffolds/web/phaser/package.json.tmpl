{
  "name": "${PROJECT_NAME}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^${PHASER_VERSION}"
  },
  "devDependencies": {
    "typescript": "^${TYPESCRIPT_VERSION}",
    "vite": "^5.0"
  }
}
