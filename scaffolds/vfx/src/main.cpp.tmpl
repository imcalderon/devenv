// ${PROJECT_NAME} - VFX Platform ${VFX_PLATFORM_YEAR} project

#include <Imath/ImathVec.h>
#include <iostream>

int main() {
    Imath::V3f position(1.0f, 2.0f, 3.0f);
    std::cout << "${PROJECT_NAME} running on VFX Platform ${VFX_PLATFORM_YEAR}\n";
    std::cout << "Position: " << position << "\n";
    return 0;
}
