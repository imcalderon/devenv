load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library")

# Compile QML Resources manually using rcc from conda
genrule(
    name = "qrc_cpp",
    srcs = ["src/resources.qrc", "src/main.qml"],
    outs = ["src/qrc_resources.cpp"],
    cmd_bat = "rcc $(location src/resources.qrc) -o $@",
)

cc_library(
    name = "qml_resources",
    srcs = [":qrc_cpp"],
    deps = ["@vfx_platform//:qt6"],
)

cc_binary(
    name = "${PROJECT_NAME}",
    srcs = [
        "src/main.cpp",
    ],
    copts = select({
        "@bazel_tools//src/conditions:windows": ["/std:c++17", "/Zc:__cplusplus", "/permissive-"],
        "//conditions:default": ["-std=c++17"],
    }),
    deps = [
        ":qml_resources",
        "@vfx_platform//:usd",
        "@vfx_platform//:qt6",
        "@vfx_platform//:imath",
        "@vfx_platform//:openexr",
        "@vfx_platform//:boost",
        "@vfx_platform//:tbb",
    ],
)

